export enum STATES_ENUM {
  E = 'E',
  D = 'D',
  A = 'A',
}

export enum STATES_TEXT_ENUM {
  E = 'Habilitado',
  D = 'Deshabilitado',
  A = 'Archivado',
}

export enum STATUS_ENUM {
  A = 'A',
  I = 'I',
}
